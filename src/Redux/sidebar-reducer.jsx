let initialState = {
     friends: [{id: 1, name: 'Roma'},
          {id: 2, name: 'Vlad'},
          {id: 3, name: 'Dasha'},
     ]
}

const sidebarReducer = (state = initialState, action) => {


     return state
}

export default sidebarReducer
